# Core Framework
fastapi>=0.121.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.6

# Database
motor>=3.7.0
beanie>=2.0.0
pymongo>=4.6.0

# Authentication & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.1.2  # Note: 5.x has compatibility issues with passlib 1.7.4

# Environment & Configuration
pydantic>=2.12.0
pydantic-settings>=2.11.0
python-dotenv>=1.0.0

# Email
email-validator>=2.3.0

# Utilities
httpx>=0.26.0

# AI & ML (Required for resume parsing and recommendations)
openai>=1.10.0
numpy>=1.26.0

# Document Processing (Required for resume/document parsing)
pypdf2>=3.0.1
python-docx>=1.1.0

# File Operations (Required for async file handling)
aiofiles>=23.2.1

# Email SMTP (Required for email notifications)
aiosmtplib>=3.0.1

# AI Orchestration & Providers (Required for AI provider fallback)
langchain>=0.1.4
langchain-core>=0.1.0
langchain-openai>=0.0.5
langchain-anthropic>=0.1.0
tiktoken>=0.5.2

# Optional - Vector Store (uncomment if using ChromaDB for semantic search)
# chromadb>=0.4.22
# numpy>=1.26.0

# Optional - for email notifications
# aiosmtplib>=3.0.1

# Optional - for background tasks
# celery>=5.3.6
# redis>=5.0.1

